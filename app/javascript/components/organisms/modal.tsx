import * as React from 'react'
import styled from 'styled-components'

const Modal = () => {
  return <StyledTxt>a</StyledTxt>
}

export default Modal

const StyledTxt = styled.div`
  &.center {
    text-align: center;
  }
`
